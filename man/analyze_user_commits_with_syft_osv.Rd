% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Commit-dependency-analysis.R
\name{analyze_user_commits_with_syft_osv}
\alias{analyze_user_commits_with_syft_osv}
\title{Analyze user repositories commit-by-commit with targeted dependency scans.}
\usage{
analyze_user_commits_with_syft_osv(
  profile,
  osv_db,
  token = NULL,
  syft_path = "syft.exe",
  dependency_patterns = default_dependency_manifest_patterns(),
  max_repos = NULL,
  max_commits_per_repo = NULL,
  include_uncertain = TRUE,
  keep_snapshots = FALSE,
  temp_root = tempdir(),
  force_scan = FALSE,
  parallel_strategy = c("manual", "auto", "staged_all"),
  parallel_repos = FALSE,
  repo_workers = 1L,
  auto_workers = NULL,
  auto_min_repos_for_repo_parallel = 3L,
  auto_min_commits_for_commit_parallel = 25L,
  syft_timeout_sec = 0L,
  syft_excludes = NULL,
  syft_scan_parallel = FALSE,
  syft_scan_workers = 1L,
  commit_info_parallel = FALSE,
  commit_info_workers = 4L,
  debug = FALSE,
  debug_repo_every = 25L,
  conn = NULL,
  clickhouse_incremental = TRUE,
  incremental_compare_commits_by_sha = FALSE,
  clickhouse_cache_prefix = "github_commit_scan",
  ...
)
}
\arguments{
\item{profile}{GitHub username or profile URL.}

\item{osv_db}{Prepared OSV DB object or path.}

\item{token}{GitHub token (recommended to avoid strict API limits).}

\item{syft_path}{Path to `syft` executable.}

\item{dependency_patterns}{Regex patterns for dependency-manifest files.}

\item{max_repos}{Optional max repositories to process.}

\item{max_commits_per_repo}{Optional max commits per repository.}

\item{include_uncertain}{Include uncertain version matches from OSV.}

\item{keep_snapshots}{Keep downloaded commit snapshots.}

\item{temp_root}{Temp directory for commit snapshots.}

\item{force_scan}{If `TRUE`, run Syft scan even when dependency files were not changed.}

\item{parallel_strategy}{Parallel strategy:
`manual` (use flags as-is), `auto` (choose one safe parallel axis),
or `staged_all` (parallel commit prefetch stage, then parallel repo scan stage).}

\item{parallel_repos}{If `TRUE`, process repositories in parallel.}

\item{repo_workers}{Number of worker processes for repository-level parallelism.}

\item{auto_workers}{Optional worker budget for `parallel_strategy = "auto"`.}

\item{auto_min_repos_for_repo_parallel}{Minimum repos to prefer repository-level parallelism in auto mode.}

\item{auto_min_commits_for_commit_parallel}{Minimum expected commits to enable commit-details parallel prefetch in auto mode.}

\item{syft_timeout_sec}{Timeout for each syft run in seconds (`0` means no timeout).}

\item{syft_excludes}{Character vector of syft `--exclude` patterns.}

\item{syft_scan_parallel}{If `TRUE`, run Syft scans in parallel within a
repository scan.}

\item{syft_scan_workers}{Number of worker processes for parallel Syft scans.}

\item{commit_info_parallel}{If `TRUE`, fetch commit detail payloads in parallel per repository.}

\item{commit_info_workers}{Number of workers for commit-details prefetch.}

\item{debug}{If `TRUE`, print stage-by-stage debug messages.}

\item{debug_repo_every}{Progress print step (in commits) per repository when `debug = TRUE`.}

\item{conn}{Optional ClickHouse connection object for ETL `get_github_repos()`.}

\item{clickhouse_incremental}{If `TRUE` (default), use ClickHouse cache for incremental analysis.}

\item{incremental_compare_commits_by_sha}{If `TRUE`, incremental mode refreshes commit list
and compares commit SHAs with cache (ignores `since` optimization) to scan only truly new commits.}

\item{clickhouse_cache_prefix}{Prefix for ClickHouse cache tables.}

\item{...}{Reserved for compatibility with earlier wrappers; currently ignored.}
}
\value{
List with `summary`, `vulnerabilities`, `vulnerability_lifecycle`, `errors`.
}
\description{
The function checks each commit in each repository and runs Syft+OSV scan only
when dependency-manifest files were changed in that commit.
}
