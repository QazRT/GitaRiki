% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MCP-OpenAI.R
\name{mcp_openai_chat}
\alias{mcp_openai_chat}
\title{Run an OpenAI-compatible chat with MCP-like tool calling.}
\usage{
mcp_openai_chat(
  messages,
  tools = list(),
  model = Sys.getenv("OPENAI_MODEL", unset = "gpt-4.1-mini"),
  api_key = Sys.getenv("OPENAI_API_KEY", unset = ""),
  base_url = Sys.getenv("OPENAI_BASE_URL", unset = "https://api.openai.com/v1"),
  system_prompt = NULL,
  temperature = 0,
  max_tokens = NULL,
  tool_choice = "auto",
  max_tool_rounds = 4,
  timeout = 120,
  extra_body = list(),
  progressbar = function(value, label = NULL) NULL,
  verbose_tools = FALSE
)
}
\arguments{
\item{messages}{List of OpenAI-compatible messages.}

\item{tools}{List of tools created by `mcp_tool()`.}

\item{model}{Model name.}

\item{api_key}{API key. If empty, no Authorization header is sent.}

\item{base_url}{API base URL.}

\item{system_prompt}{Optional system prompt prepended to `messages`.}

\item{temperature}{Sampling temperature.}

\item{max_tokens}{Optional max output tokens.}

\item{tool_choice}{OpenAI-compatible tool choice value.}

\item{max_tool_rounds}{Max number of tool-call iterations.}

\item{timeout}{Timeout in seconds.}

\item{extra_body}{Extra request body fields.}

\item{progressbar}{Callback function called with progress percentage and an
optional label during tool-call rounds.}

\item{verbose_tools}{If `TRUE`, prints tool calls as they are executed.}
}
\value{
List with `messages`, last `response`, `final_message`, and
  `tool_events`.
}
\description{
Run an OpenAI-compatible chat with MCP-like tool calling.
}
